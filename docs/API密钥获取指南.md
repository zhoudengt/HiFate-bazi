# API密钥获取指南

## 📋 目录

1. [极速数据 (jisuapi)](#1-极速数据-jisuapi)
2. [天聚数行 (tianapi)](#2-天聚数行-tianapi)
3. [六派数据 (6api)](#3-六派数据-6api)
4. [配置API密钥](#4-配置api密钥)
5. [测试API](#5-测试api)

---

## 1. 极速数据 (jisuapi)

### 1.1 注册步骤

1. **访问官网**
   - 网址：https://www.jisuapi.com
   - 或搜索：极速数据 API

2. **注册账号**
   - 点击"注册"或"免费注册"
   - 填写手机号、验证码、密码
   - 完成注册

3. **获取API密钥**
   - 登录后进入"控制台"或"我的API"
   - 找到"星座运势"相关API
   - 点击"获取密钥"或"查看密钥"
   - 复制 API Key（appkey）

4. **免费额度**
   - 免费会员：每天 100 次调用
   - 白银会员：每天 600 次调用（付费）
   - 钻石会员：每天 15 万次调用（付费）

### 1.2 配置

```bash
export JISUAPI_KEY="your_jisuapi_appkey"
```

或在 `.env` 文件中：

```bash
JISUAPI_KEY=your_jisuapi_appkey
```

---

## 2. 天聚数行 (tianapi)

### 2.1 注册步骤

1. **访问官网**
   - 网址：https://www.tianapi.com
   - 或搜索：天聚数行 API

2. **注册账号**
   - 点击"注册"或"免费注册"
   - 填写邮箱或手机号、验证码、密码
   - 完成注册

3. **获取API密钥**
   - 登录后进入"控制台"或"API管理"
   - 找到"星座运势"相关API
   - 点击"申请接口"或"获取密钥"
   - 复制 API Key

4. **免费额度**
   - 普通会员：每天 100 次调用
   - 高级会员：每天 1 万次调用（付费）
   - 黄金会员：每天 50 万次调用（付费）

### 2.2 配置

```bash
export TIANAPI_KEY="your_tianapi_key"
```

或在 `.env` 文件中：

```bash
TIANAPI_KEY=your_tianapi_key
```

---

## 3. 六派数据 (6api)

### 3.1 注册步骤

1. **访问官网**
   - 网址：https://www.6api.net
   - 或搜索：六派数据 API

2. **注册账号**
   - 点击"注册"或"免费注册"
   - 填写手机号、验证码、密码
   - 完成注册

3. **获取API密钥**
   - 登录后进入"控制台"或"API管理"
   - 找到"星座运势"相关API
   - 点击"获取密钥"或"查看密钥"
   - 复制 API Key（appkey）

4. **免费额度**
   - 免费会员：每天 100 次调用
   - 白银会员：每天 600 次调用（付费）
   - 钻石会员：每天 15 万次调用（付费）

### 3.2 配置

```bash
export API6API_KEY="your_6api_appkey"
```

或在 `.env` 文件中：

```bash
API6API_KEY=your_6api_appkey
```

---

## 4. 配置API密钥

### 4.1 方式1：环境变量（推荐）

```bash
# 在终端中设置（临时，关闭终端后失效）
export JISUAPI_KEY="your_api_key"

# 或在 ~/.bashrc 或 ~/.zshrc 中添加（永久）
echo 'export JISUAPI_KEY="your_api_key"' >> ~/.zshrc
source ~/.zshrc
```

### 4.2 方式2：.env 文件（推荐用于开发）

在项目根目录创建或编辑 `.env` 文件：

```bash
# 运势API配置（选择其中一个）
JISUAPI_KEY=your_jisuapi_key
# TIANAPI_KEY=your_tianapi_key
# API6API_KEY=your_6api_key
```

### 4.3 方式3：系统环境变量（生产环境）

```bash
# Linux/Mac
sudo vi /etc/environment
# 添加：JISUAPI_KEY=your_api_key

# 或使用 systemd
sudo systemctl edit your-service
# 添加环境变量配置
```

---

## 5. 测试API

### 5.1 检查配置

```bash
# 检查环境变量
echo $JISUAPI_KEY

# 或检查 .env 文件
cat .env | grep JISUAPI_KEY
```

### 5.2 测试接口

```bash
# 测试获取今日运势
curl -X POST http://localhost:8001/api/v1/fortune/daily \
  -H "Content-Type: application/json" \
  -d '{
    "constellation": "白羊座"
  }'

# 测试获取API提供商列表
curl http://localhost:8001/api/v1/fortune/providers
```

### 5.3 查看日志

如果API调用失败，查看日志：

```bash
# 查看服务器日志
tail -f logs/web_app_8001.log

# 或查看控制台输出
```

---

## 6. 常见问题

### Q1: 注册时收不到验证码？

**A**: 
- 检查手机号是否正确
- 检查是否被拦截（垃圾短信）
- 等待几分钟后重试
- 联系客服

### Q2: API调用返回错误？

**A**: 
- 检查API密钥是否正确
- 检查是否超出免费额度
- 查看错误信息中的具体提示
- 检查网络连接

### Q3: 如何查看调用次数？

**A**: 
- 登录API提供商的控制台
- 查看"调用统计"或"使用情况"
- 查看剩余调用次数

### Q4: 免费额度不够用怎么办？

**A**: 
- 升级到付费会员
- 使用多个API提供商（配置多个密钥）
- 添加缓存机制（减少API调用）

---

## 7. 快速开始

### 步骤1：选择API提供商

推荐使用**极速数据 (jisuapi)**，因为：
- 注册简单
- 免费额度充足（100次/天）
- 接口稳定

### 步骤2：注册并获取密钥

1. 访问 https://www.jisuapi.com
2. 注册账号
3. 获取API密钥

### 步骤3：配置密钥

```bash
# 在项目根目录的 .env 文件中添加
echo 'JISUAPI_KEY=your_api_key_here' >> .env
```

### 步骤4：重启服务

```bash
# 重启服务使配置生效
python server/start.py
```

### 步骤5：测试

```bash
curl -X POST http://localhost:8001/api/v1/fortune/daily \
  -H "Content-Type: application/json" \
  -d '{"constellation": "白羊座"}'
```

---

**提示**: 如果暂时无法获取真实API密钥，可以使用测试模式（见下面的测试服务说明）

---

**文档版本**: v1.0  
**最后更新**: 2025-01-XX

