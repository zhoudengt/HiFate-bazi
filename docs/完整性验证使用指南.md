# 完整性验证使用指南

## 概述

完整性验证系统解决交付质量丢三落四的问题，自动检查所有必需文件、注册、测试是否完成，生成详细的完整性报告。

## 核心功能

1. **检查所有必需文件是否创建**
2. **检查所有必需注册是否完成**
3. **检查所有必需测试是否编写**
4. **生成完整性报告**

## 使用方法

### API 开发完整性验证

```bash
# 验证 API 开发完整性
python3 scripts/ai/completeness_validator.py --type api --name xxx

# 输出 JSON 格式
python3 scripts/ai/completeness_validator.py --type api --name xxx --json
```

**检查项**：
- API 文件是否创建（`server/api/v1/xxx.py`）
- gRPC 端点是否注册（`grpc_gateway.py`）
- 路由是否注册（`main.py`）
- Pydantic 模型是否定义
- 测试文件是否创建（`tests/unit/test_xxx.py`）

### 规则开发完整性验证

```bash
# 验证规则开发完整性
python3 scripts/ai/completeness_validator.py --type rule --name wealth

# 输出 JSON 格式
python3 scripts/ai/completeness_validator.py --type rule --name wealth --json
```

**检查项**：
- 导入脚本是否创建（`scripts/migration/import_xxx_rules.py`）
- 数据库导入脚本是否创建（`scripts/migration/import_xxx_rules_to_db.py`）
- 前端类型映射是否添加（`formula-analysis.html`）
- 后端类型映射是否添加（`formula_analysis.py`）

### 前端开发完整性验证

```bash
# 验证前端开发完整性
python3 scripts/ai/completeness_validator.py --type frontend --name fortune

# 输出 JSON 格式
python3 scripts/ai/completeness_validator.py --type frontend --name fortune --json
```

**检查项**：
- HTML 文件是否创建（`local_frontend/xxx.html`）
- JS 文件是否创建（`local_frontend/js/xxx.js`）
- CSS 文件是否创建（可选）
- API 对接是否实现

## 报告格式

### 文本报告

```
================================================================================
📋 完整性验证报告
================================================================================

开发类型：api
名称：xxx
完整性：80.0% (4/5)

✅ 已完成的项：
  ✅ API 文件存在: server/api/v1/xxx.py
  ✅ gRPC 端点已注册
  ✅ 路由已注册
  ✅ Pydantic 模型已定义

❌ 缺失的项：
  ❌ 测试文件不存在: tests/unit/test_xxx.py

================================================================================
```

### JSON 报告

```json
{
  "type": "api",
  "name": "xxx",
  "checks": {
    "api_file": true,
    "grpc_registration": true,
    "router_registration": true,
    "pydantic_model": true,
    "test_file": false
  },
  "missing": [
    "测试文件不存在: tests/unit/test_xxx.py"
  ],
  "passed": [
    "API 文件存在: server/api/v1/xxx.py",
    "gRPC 端点已注册",
    "路由已注册",
    "Pydantic 模型已定义"
  ],
  "completeness": 80.0,
  "total_checks": 5,
  "passed_checks": 4
}
```

## 使用流程

### 开发完成后验证

```bash
# 1. 开发代码...

# 2. 运行完整性验证
python3 scripts/ai/completeness_validator.py --type api --name xxx

# 3. 根据报告修复缺失的项

# 4. 重新验证
python3 scripts/ai/completeness_validator.py --type api --name xxx

# 5. 完整性达到 100% 后，完成开发
python3 scripts/ai/dev_assistant.py --complete
```

### 集成到开发流程

**使用智能检查清单**：
```bash
# 运行检查清单（自动调用完整性验证器）
python3 scripts/ai/smart_checklist.py --type api --name xxx
```

**使用开发助手**：
```bash
# 完成开发（自动运行完整性验证）
python3 scripts/ai/dev_assistant.py --complete
```

## 常见问题

### Q1: 完整性验证失败？

**A**: 查看缺失的项：
```bash
python3 scripts/ai/completeness_validator.py --type api --name xxx
```

根据报告修复缺失的项，重新验证。

### Q2: 某些检查项不适用？

**A**: 完整性验证器会检查所有必需项，如果某些项不适用，可以：
1. 创建占位文件（如空的测试文件）
2. 修改验证器代码，添加例外规则

### Q3: 如何添加新的检查项？

**A**: 修改 `scripts/ai/completeness_validator.py`：
1. 在对应的验证方法中添加新的检查逻辑
2. 更新检查清单

## 最佳实践

1. **开发完成后立即验证**：确保不遗漏任何步骤
2. **完整性达到 100% 后再提交**：避免交付丢三落四
3. **使用智能检查清单**：自动加载对应检查清单
4. **集成到开发流程**：使用开发助手完成流程

## 相关文档

- **智能开发助手使用指南**：`docs/智能开发助手使用指南.md`
- **智能检查清单**：`scripts/ai/smart_checklist.py`

