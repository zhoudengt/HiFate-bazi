# Docker 代码保护快速参考

## 🚀 快速开始

### 构建保护后的镜像

```bash
docker build --platform linux/amd64 -t hifate-bazi:protected .
```

### 验证保护效果

```bash
./scripts/docker/verify_protection.sh hifate-bazi:protected
```

### 测试运行

```bash
docker run -d --name test -p 8001:8001 hifate-bazi:protected
curl http://localhost:8001/health
```

## 🔍 验证清单

- [ ] 原始 .py 文件已完全删除
- [ ] 混淆后的文件存在（pyarmor_runtime）
- [ ] 混淆后的代码包含保护标记
- [ ] 模块可以正常导入
- [ ] 服务可以正常运行

## 📝 关键文件

| 文件 | 说明 |
|------|------|
| `Dockerfile` | 包含 PyArmor 混淆配置 |
| `scripts/docker/verify_protection.sh` | 验证保护效果脚本 |
| `scripts/docker/obfuscate.sh` | 本地混淆测试脚本 |
| `docs/Docker代码保护方案.md` | 完整文档 |

## ⚠️ 重要提示

1. **原始代码备份**：确保代码已提交到 Git
2. **性能影响**：可能有 5-10% 性能开销
3. **平台限制**：只能在 linux.x86_64 运行
4. **调试困难**：混淆后难以调试，保留开发版本

## 🔗 相关文档

- [完整保护方案文档](./Docker代码保护方案.md)
- [Docker 部署指南](./Docker生产部署完整指南.md)

