# 前端功能与后端接口对应表

**创建日期**：2025-01-15  
**用途**：列出前端功能对应的后端API接口

---

## 📋 功能与接口对应表

### 1. 🔮 智能运势分析

**前端页面**：
- `smart-fortune.html` - 智能运势分析页面
- `smart-fortune-stream.html` - 智能运势分析流式页面
- `ai-qa.html` - AI问答页面

**后端接口**：
- **流式接口**（推荐）：`GET /api/v1/smart-fortune/smart-analyze-stream`
  - **方法**：GET
  - **参数**：Query参数
    - `question`: 用户问题（必填）
    - `year`: 出生年份（必填）
    - `month`: 出生月份（必填）
    - `day`: 出生日期（必填）
    - `hour`: 出生时辰（可选，默认12）
    - `gender`: 性别（必填，male/female）
    - `user_id`: 用户ID（可选）
    - `include_fortune_context`: 是否包含流年大运分析（可选，默认false）
  - **返回**：SSE流式响应
  - **文件位置**：`server/api/v1/smart_fortune.py`

**特点**：
- 基于用户问题智能分析运势
- 支持流式返回，实时显示结果
- 可结合八字、规则匹配、意图识别等多个维度

---

### 2. 📅 大运分析

**前端页面**：
- `dayun.html` - 大运分析页面
- `fortune.html` - 大运流年流月整合页面

**后端接口**：
- **显示接口**：`POST /api/v1/bazi/dayun/display`
  - **方法**：POST
  - **请求体**：
    ```json
    {
      "solar_date": "1990-05-15",
      "solar_time": "14:30",
      "gender": "male",
      "current_time": "2025-01-15 10:00:00",  // 可选
      "dayun_index": 4,  // 可选（已废弃，优先使用dayun_year_start和dayun_year_end）
      "dayun_year_start": 2025,  // 可选
      "dayun_year_end": 2035  // 可选
    }
    ```
  - **返回**：
    ```json
    {
      "success": true,
      "dayun": {
        "current": {...},
        "list": [...],
        "qiyun": {...},
        "jiaoyun": {...}
      }
    }
    ```
  - **文件位置**：`server/api/v1/bazi_display.py`

**特点**：
- 计算并展示用户的大运信息
- 包含起运、交运信息
- 支持指定大运范围查询

---

### 3. 📊 流年分析

**前端页面**：
- `liunian.html` - 流年分析页面
- `fortune.html` - 大运流年流月整合页面

**后端接口**：
- **显示接口**：`POST /api/v1/bazi/liunian/display`
  - **方法**：POST
  - **请求体**：
    ```json
    {
      "solar_date": "1990-05-15",
      "solar_time": "14:30",
      "gender": "male",
      "year_range": {  // 可选
        "start": 2020,
        "end": 2030
      },
      "dayun_year_start": 2025,  // 可选（指定大运范围）
      "dayun_year_end": 2035  // 可选
    }
    ```
  - **返回**：
    ```json
    {
      "success": true,
      "liunian": {
        "current": {...},
        "list": [...]
      }
    }
    ```
  - **文件位置**：`server/api/v1/bazi_display.py`

**增强分析接口**（可选）：
- **接口**：`POST /api/v1/bazi/liunian-enhanced`
  - **方法**：POST
  - **请求体**：
    ```json
    {
      "solar_date": "1990-05-15",
      "solar_time": "14:30",
      "gender": "male",
      "target_year": 2025,  // 可选
      "years_ahead": 10  // 可选，默认10年
    }
    ```
  - **返回**：增强分析结果（包含吉凶评分、转折点等）
  - **文件位置**：`server/api/v1/liunian_enhanced.py`

**特点**：
- 计算并展示用户的流年信息
- 支持指定年份范围查询
- 支持在大运范围内查询流年
- 增强分析包含量化评分和转折点识别

---

### 4. 🔮 一事一卦

**前端页面**：
- `yigua.html` - 一事一卦页面

**后端接口**：
- **占卜接口**：`POST /api/v1/bazi/yigua/divinate`
  - **方法**：POST
  - **请求体**：
    ```json
    {
      "question": "我这次投资能成功吗？",
      "timestamp": "2025-01-15T10:00:00"  // 可选，ISO格式
    }
    ```
  - **返回**：
    ```json
    {
      "success": true,
      "question": "我这次投资能成功吗？",
      "gua": {
        "number": 11,
        "name": "泰",
        "pinyin": "tài",
        "meaning": "天地",
        "description": "小往大来，吉亨"
      },
      "interpretation": "【卦象】泰卦（tài）\n【解读】...",
      "timestamp": "2025-01-15T10:00:00"
    }
    ```
  - **文件位置**：`server/api/v1/yigua.py`

- **获取卦列表接口**：`GET /api/v1/bazi/yigua/list`
  - **方法**：GET
  - **返回**：六十四卦列表
  - **文件位置**：`server/api/v1/yigua.py`

**特点**：
- 基于《周易》六十四卦进行占卜
- 本地计算，无需调用LLM（<100ms）
- 确定性：相同问题在同一时间得到相同结果

---

### 5. 📆 今日运势（基于八字）

**前端页面**：
- `bazi-daily-fortune.html` - 今日运势页面
- `daily-fortune-calendar.html` - 每日运势日历页面

**后端接口**：
- **今日运势接口**：`POST /api/v1/bazi/daily-fortune`
  - **方法**：POST
  - **请求体**：
    ```json
    {
      "solar_date": "1990-05-15",
      "solar_time": "14:30",
      "gender": "male",
      "target_date": "2025-01-15",  // 可选，默认为今天
      "use_llm": false,  // 可选，默认false（使用规则匹配）
      "calendar_type": "solar",  // 可选，solar/lunar
      "location": "北京",  // 可选
      "latitude": 39.9042,  // 可选
      "longitude": 116.4074  // 可选
    }
    ```
  - **返回**：
    ```json
    {
      "success": true,
      "target_date": "2025-01-15",
      "fortune": {
        "overall": "整体运势分析...",
        "career": "事业运势...",
        "wealth": "财运...",
        "love": "感情运势...",
        "health": "健康运势..."
      }
    }
    ```
  - **文件位置**：`server/api/v1/daily_fortune.py`

- **每日运势日历接口**：`POST /api/v1/daily-fortune-calendar/query`
  - **方法**：POST
  - **请求体**：同今日运势接口
  - **返回**：日历格式的运势数据
  - **文件位置**：`server/api/v1/daily_fortune_calendar.py`

**特点**：
- 结合用户的八字信息分析今日运势
- 结合流年、流月、流日信息
- 支持规则匹配和LLM生成两种模式
- 快速响应（规则匹配模式 <1秒）

---

### 6. 📅 当月运势（基于八字）

**前端页面**：
- `bazi-monthly-fortune.html` - 当月运势页面

**后端接口**：
- **当月运势接口**：`POST /api/v1/bazi/monthly-fortune`
  - **方法**：POST
  - **请求体**：
    ```json
    {
      "solar_date": "1990-05-15",
      "solar_time": "14:30",
      "gender": "male",
      "target_year": 2025,  // 可选，默认为今年
      "target_month": 1,  // 可选，默认为本月（1-12）
      "calendar_type": "solar",  // 可选，solar/lunar
      "location": "北京",  // 可选
      "latitude": 39.9042,  // 可选
      "longitude": 116.4074  // 可选
    }
    ```
  - **返回**：
    ```json
    {
      "success": true,
      "target_year": 2025,
      "target_month": 1,
      "fortune": {
        "overall": "整体运势分析...",
        "career": "事业运势...",
        "wealth": "财运...",
        "love": "感情运势...",
        "health": "健康运势..."
      }
    }
    ```
  - **文件位置**：`server/api/v1/monthly_fortune.py`

**特点**：
- 结合用户的八字信息分析当月运势
- 结合流年、流月信息
- 支持指定年份和月份查询

---

### 7. 👤 面相分析

**前端页面**：
- `face-analysis.html` - 面相分析页面（V1）
- `face-analysis-v2.html` - 面相分析页面（V2，推荐）

**后端接口**：

#### V1接口（旧版，gRPC）
- **接口**：`POST /api/v1/fortune/face/analyze`
  - **方法**：POST
  - **Content-Type**：`multipart/form-data`
  - **请求参数**：
    - `image`: 头部面相照片（File，必填）
    - `solar_date`: 阳历日期 YYYY-MM-DD（可选）
    - `solar_time`: 出生时间 HH:MM（可选）
    - `gender`: 性别 male/female（可选）
    - `use_bazi`: 是否结合八字分析（可选，默认true）
  - **返回**：
    ```json
    {
      "success": true,
      "data": {
        "report": "面相分析报告..."
      }
    }
    ```
  - **文件位置**：`server/api/v1/fortune_analysis.py`
  - **流式接口**：`POST /api/v1/fortune/face/analyze/stream`（需要认证）

#### V2接口（新版，推荐）
- **接口**：`POST /api/v2/face/analyze`
  - **方法**：POST
  - **Content-Type**：`multipart/form-data`
  - **请求参数**：
    - `image`: 面相照片（File，必填）
    - `analysis_types`: 分析类型（可选，默认"gongwei,liuqin,shishen"）
      - 可选值：`gongwei`（宫位）、`liuqin`（六亲）、`shishen`（十神）、`features`（特征）
    - `birth_year`: 出生年份（可选）
    - `birth_month`: 出生月份（可选）
    - `birth_day`: 出生日期（可选）
    - `birth_hour`: 出生时辰（可选）
    - `gender`: 性别（可选）
  - **返回**：
    ```json
    {
      "success": true,
      "data": {
        "face_detected": true,
        "landmarks": {...},
        "santing_analysis": {...},
        "wuyan_analysis": {...},
        "gongwei_analysis": [...],
        "liuqin_analysis": [...],
        "shishen_analysis": [...],
        "overall_summary": "综合分析..."
      }
    }
    ```
  - **文件位置**：`server/api/v2/face_analysis.py`

**特点**：
- 支持上传人脸照片进行面相分析
- V2版本支持多种分析类型（宫位、六亲、十神等）
- 可选结合八字信息进行综合分析
- V2版本功能更强大，推荐使用

---

## 📊 接口汇总表

| 功能 | 前端页面 | 后端接口 | 方法 | 文件位置 |
|------|----------|----------|------|----------|
| 智能运势分析 | `smart-fortune.html` | `/api/v1/smart-fortune/smart-analyze-stream` | GET | `server/api/v1/smart_fortune.py` |
| 大运分析 | `dayun.html` | `/api/v1/bazi/dayun/display` | POST | `server/api/v1/bazi_display.py` |
| 流年分析 | `liunian.html` | `/api/v1/bazi/liunian/display` | POST | `server/api/v1/bazi_display.py` |
| 流年增强分析 | - | `/api/v1/bazi/liunian-enhanced` | POST | `server/api/v1/liunian_enhanced.py` |
| 一事一卦 | `yigua.html` | `/api/v1/bazi/yigua/divinate` | POST | `server/api/v1/yigua.py` |
| 今日运势 | `bazi-daily-fortune.html` | `/api/v1/bazi/daily-fortune` | POST | `server/api/v1/daily_fortune.py` |
| 每日运势日历 | `daily-fortune-calendar.html` | `/api/v1/daily-fortune-calendar/query` | POST | `server/api/v1/daily_fortune_calendar.py` |
| 当月运势 | `bazi-monthly-fortune.html` | `/api/v1/bazi/monthly-fortune` | POST | `server/api/v1/monthly_fortune.py` |
| 面相分析（V1） | `face-analysis.html` | `/api/v1/fortune/face/analyze` | POST | `server/api/v1/fortune_analysis.py` |
| 面相分析（V2） | `face-analysis-v2.html` | `/api/v2/face/analyze` | POST | `server/api/v2/face_analysis.py` |

---

## 🔧 接口调用示例

### 1. 智能运势分析（流式）

```javascript
const url = `${API_CONFIG.baseURL}/smart-fortune/smart-analyze-stream?` +
    `question=${encodeURIComponent(question)}&` +
    `year=${year}&month=${month}&day=${day}&hour=${hour}&gender=${gender}`;

const eventSource = new EventSource(url);
eventSource.onmessage = (event) => {
    const data = JSON.parse(event.data);
    // 处理流式数据
};
```

### 2. 大运分析

```javascript
const response = await api.post('/bazi/dayun/display', {
    solar_date: '1990-05-15',
    solar_time: '14:30',
    gender: 'male'
});
```

### 3. 流年分析

```javascript
const response = await api.post('/bazi/liunian/display', {
    solar_date: '1990-05-15',
    solar_time: '14:30',
    gender: 'male',
    year_range: {
        start: 2020,
        end: 2030
    }
});
```

### 4. 一事一卦

```javascript
const response = await api.post('/bazi/yigua/divinate', {
    question: '我这次投资能成功吗？'
});
```

### 5. 今日运势

```javascript
const response = await api.post('/bazi/daily-fortune', {
    solar_date: '1990-05-15',
    solar_time: '14:30',
    gender: 'male',
    target_date: '2025-01-15'
});
```

### 6. 当月运势

```javascript
const response = await api.post('/bazi/monthly-fortune', {
    solar_date: '1990-05-15',
    solar_time: '14:30',
    gender: 'male',
    target_year: 2025,
    target_month: 1
});
```

### 7. 面相分析（V2）

```javascript
const formData = new FormData();
formData.append('image', imageFile);
formData.append('analysis_types', 'gongwei,liuqin,shishen');
formData.append('birth_year', 1990);
formData.append('birth_month', 5);
formData.append('birth_day', 15);
formData.append('gender', 'male');

const response = await fetch(`${API_CONFIG.baseURL}/api/v2/face/analyze`, {
    method: 'POST',
    body: formData
});
```

---

## 📝 注意事项

1. **智能运势分析**：使用流式接口，需要通过EventSource接收数据
2. **大运/流年分析**：支持通过`dayun_year_start`和`dayun_year_end`指定大运范围
3. **今日/当月运势**：支持农历输入和时区转换，需要提供`calendar_type`和`location`参数
4. **面相分析**：V2版本功能更强大，推荐使用V2接口
5. **所有接口**：都需要提供基本的八字信息（solar_date, solar_time, gender）

---

## 📚 相关文档

- [API文档](../README.md)
- [前端开发文档](../local_frontend/README.md)
- [gRPC协议规范](../docs/standards/grpc-protocol.md)

---

**更新日期**：2025-01-15
