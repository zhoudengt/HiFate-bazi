# 子女学习分析 - 提示词规则说明

提示词在 **Coze（或百炼）** 的「子女学习分析」Bot 的 **System Prompt** 里改，代码库不存完整提示词。

---

## 子女去掉数量显示：加哪里、加什么

### 1. 注意事项里加一条（必加）

在 System Prompt 的 **注意事项** 末尾加：

```
子女数量：分析中不显示、不推断子女具体数量（如几个孩子、几子几女）。只分析子女缘分、子女特质、亲子关系与养育建议；不输出「子女数量」「几个孩子」等表述。
```

### 2. 第二部分改一句（建议）

找到 **第二部分 子女星与子女宫分析** 里的：

- 原文：`子女数量与质量：根据子女星的数量、位置、强弱分析子女数量可能性和子女特质。`
- 改为：`子女特质与缘分：根据子女星的位置、强弱分析子女缘分深浅与子女特质。`

---

## 两个问题（需你补充场景再查）

- **统一接口多次调用丢失部分数据**：是「同一次请求里多次调同一接口」还是「连续多次请求里部分返回不完整」？具体哪个接口、缺哪些字段？
- **多次调用**：是评测脚本连续调还是用户多次点「重新生成」？是否同一八字？

补充上述信息后可针对性查。评测脚本已有重试：`api_client.py` 里 `call_children_study_stream` 用 `_post_stream_with_retry()`。
