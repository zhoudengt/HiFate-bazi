# 问题清单 - 快速回复模板

> **使用说明**：请直接在每个问题下方填写您的答案，保持格式即可。

---

## 🔴 优先级1：必须确认（阻塞开发）

### Q1. 十神相关（财富规则）

**问题：** "年干主星"、"副星"等术语的数据映射关系

**子问题：**

**Q1.1** "年干主星是正财" 是否等于 `details['year']['main_star'] == '正财'`？
```
您的答案：是 / 否 / 其他说明
```

**Q1.2** "月干主星是正财" 是否等于 `details['month']['main_star'] == '正财'`？
```
您的答案：是 / 否 / 其他说明
```

**Q1.3** "日支副星是正财" 是否等于 `'正财' in details['day']['hidden_stars']`？
```
您的答案：是 / 否 / 其他说明
```

**Q1.4** "副星有正官"（未指定柱）是指：
- A. 全部四柱的 `hidden_stars` 中**任意一个**包含正官
- B. 某个特定柱的 `hidden_stars` 包含正官（请说明是哪个柱）
- C. 其他规则（请说明）
```
您的答案：A / B / C，说明：
```

---

### Q2. 地支关系（身体规则）

**问题：** "子午冲"、"丑未冲"等地支关系如何判断

**Q2.1** 是否使用 `relationships['branch_relations']['chong']` 来判断？
```
您的答案：是 / 否 / 其他说明
```

**Q2.2** 如果是，匹配逻辑是：
```python
# 示例：子午冲
chong_list = relationships['branch_relations']['chong']
# chong_list = [{"pillars": ["year", "hour"], "branches": ["子", "午"]}]

# 判断方式：只要 branches 中包含 ['子', '午'] 即匹配？
```
```
您的答案：是 / 否 / 其他说明
```

---

### Q3. 五行统计（身体规则）

**问题：** "对应五行属性火低于1个（包含1个）" 如何判断

**Q3.1** 是否直接使用 `element_counts['火'] <= 1`？
```
您的答案：是 / 否 / 其他说明
```

**Q3.2** `element_counts` 是否已包含天干+地支的五行统计？
```
您的答案：是 / 否 / 其他说明
```

**Q3.3** "低于1个（包含1个）"的含义：
- A. `<= 1`（0或1个都满足）
- B. `< 1`（只有0个满足）
- C. 其他含义
```
您的答案：A / B / C，说明：
```

---

### Q4. 年柱/日柱条件（总评/性格规则）

**问题：** 复合条件中的季节和时辰定义

**Q4.1** "出生于春季"的定义：
- A. 农历1-3月
- B. 阳历2-4月
- C. 节气：立春-立夏
- D. 其他定义
```
您的答案：A / B / C / D，说明：
```

**Q4.2** "出生于夏季"、"秋季"、"冬季"的定义（请按Q4.1的格式说明）：
```
您的答案：
夏季 = 
秋季 = 
冬季 = 
```

**Q4.3** "出生于卯时到申时"是否等于时辰地支 `in ['卯','辰','巳','午','未','申']`？
```
您的答案：是 / 否 / 其他说明
```

**Q4.4** "出生于酉时到寅时"是否等于时辰地支 `in ['酉','戌','亥','子','丑','寅']`（跨日）？
```
您的答案：是 / 否 / 其他说明
```

**Q4.5** "出生于农历六月"如何判断？当前数据中是否有农历月份？
```
您的答案：
```

---

### Q8. 特殊术语定义

**问题：** 特殊命理术语的判断方式

**Q8.1** "身旺"、"极旺"如何判断：
- A. 调用 `/api/v1/bazi/wangshuai` 的 `status` 字段
- B. 使用其他方法（请说明）
```
您的答案：A / B，说明：
```

**Q8.2** 如果是A，`status` 字段的可能值有哪些？
```
您的答案：
示例：身旺、极旺、身弱、极弱 等
```

**Q8.3** "禄"临官 如何判断？
```
您的答案：
```

**Q8.4** "长生" 如何判断？（是否指十二长生？）
```
您的答案：
```

**Q8.5** "库" 如何判断？（是否指四库：辰戌丑未？）
```
您的答案：
```

**Q8.6** "不受刑冲" 如何判断？
- A. `relationships['branch_relations']['chong']` 为空，且 `['xing']` 为空
- B. 针对特定柱判断（请说明）
- C. 其他方式
```
您的答案：A / B / C，说明：
```

---

## 🟡 优先级2：数据补充

### Q6. 结果列缺失数据

**问题：** 性格、总评、身体规则的结果列为None

**Q6.1** 是否需要补充这些数据？
```
您的答案：是 / 否 / 暂不需要
```

**Q6.2** 如果需要，预计何时提供？
```
您的答案：
```

**Q6.3** 在数据补充前，是否先开发财富规则？
```
您的答案：是 / 否
```

---

### Q7. Excel表格中的两列'结果'

**问题：** 部分工作表有两列"结果"

**Q7.1** 如何处理两列"结果"？
- A. 合并显示（用换行或分段）
- B. 只使用第一列
- C. 只使用第二列
- D. 其他方式
```
您的答案：A / B / C / D，说明：
```

---

## 🟢 优先级3：开发策略

### Q5. 复合条件解析

**问题：** 复合条件的解析逻辑

**Q5.1** 条件"年干主星是正财，且副星有正官，并且还是身旺或极旺"：
- "且副星有正官" 是指：
  - A. 年柱的副星
  - B. 全部四柱的副星（任意一柱包含即可）
  - C. 其他规则
```
您的答案：A / B / C，说明：
```

**Q5.2** 多个条件之间的逻辑关系：
- "且"、"并且" = AND（全部满足）
- "或" = OR（满足其一）
- 是否正确？
```
您的答案：是 / 否 / 其他说明
```

---

## 🚀 开发策略选择

### 方案选择

**请选择开发方案：**

**方案A**（推荐）：
- 先开发财富规则（97条，数据完整）
- 验证规则引擎后，再扩展到其他规则

**方案B**：
- 等待全部数据补充后，一起开发

```
您的选择：方案A / 方案B

原因说明（可选）：
```

---

## 📋 提交方式

**请您可以：**
1. 直接在此文档中填写答案
2. 或者以消息形式回复，格式如：`Q1.1: 是, Q1.2: 是, Q1.3: 是, Q1.4: A`
3. 或者只回复最关键的问题（Q1-Q4, Q8）

**最关键的5个问题（必答）：**
- Q1.4（副星匹配规则）
- Q3.1（五行统计）
- Q4.1（季节定义）
- Q8.1（身旺判断）
- Q8.6（刑冲判断）

**回复这5个问题后，即可立即开始开发！** 🚀

