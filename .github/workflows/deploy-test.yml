# HiFate-bazi 测试环境自动部署
# 触发条件：推送到 master 分支（测试环境：123.57.216.15）
# 功能：从 GitHub Container Registry 拉取镜像并部署到测试服务器
# 
# 标准流程：
#   1. build-and-push.yml 构建镜像并推送到 GHCR
#   2. 本 workflow 从 GHCR 拉取镜像
#   3. 部署到测试服务器（零停机滚动更新）

name: 🧪 Deploy to Test Environment

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  deploy:
    name: 部署到测试服务器
    runs-on: ubuntu-latest
    
    steps:
      # 1. 检出代码
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      # 2. 设置 SSH
      - name: 🔑 Setup SSH
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.TEST_SSH_PRIVATE_KEY }}
          
      # 3. 添加服务器到已知主机
      - name: 📝 Add server to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.TEST_SERVER_HOST }} >> ~/.ssh/known_hosts
      
      # 4. 部署到测试服务器
      - name: 🚀 Deploy to test server
        run: |
          ssh ${{ secrets.TEST_SERVER_USER }}@${{ secrets.TEST_SERVER_HOST }} << 'EOF'
            set -e
            
            echo "🔄 开始部署到测试环境..."
            
            # 进入项目目录
            cd /opt/HiFate-bazi
            
            # 拉取最新代码（用于 docker-compose 配置）
            echo "📥 拉取最新代码..."
            git fetch origin
            git checkout master
            git pull origin master
            
            # 登录到 GitHub Container Registry
            echo "🔐 登录到 GitHub Container Registry..."
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin || {
              echo "❌ 无法登录到 GitHub Container Registry"
              exit 1
            }
            
            # 拉取最新镜像
            echo "📥 拉取最新镜像..."
            IMAGE_TAG="ghcr.io/${{ github.repository }}/hifate-bazi:master"
            docker pull ${IMAGE_TAG} || {
              echo "⚠️  拉取 master 标签失败，尝试 latest 标签"
              IMAGE_TAG="ghcr.io/${{ github.repository }}/hifate-bazi:latest"
              docker pull ${IMAGE_TAG} || {
                echo "❌ 无法拉取任何镜像，部署失败"
                exit 1
              }
            }
            
            echo "✅ 镜像拉取成功: ${IMAGE_TAG}"
            
            # 滚动更新（零停机部署）
            echo "🔄 滚动更新服务..."
            DOCKER_IMAGE=${IMAGE_TAG} docker compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.image.yml up -d --no-deps web
            
            # 等待新容器启动
            echo "⏳ 等待新容器启动..."
            sleep 20
            
            # 健康检查（重试机制）
            echo "🏥 健康检查..."
            MAX_RETRIES=5
            RETRY_COUNT=0
            HEALTH_CHECK_PASSED=false
            
            while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
              if curl -f -s http://localhost:8001/health > /dev/null 2>&1; then
                HEALTH_CHECK_PASSED=true
                break
              fi
              RETRY_COUNT=$((RETRY_COUNT + 1))
              echo "⏳ 健康检查失败，重试 $RETRY_COUNT/$MAX_RETRIES..."
              sleep 5
            done
            
            if [ "$HEALTH_CHECK_PASSED" = true ]; then
              echo "✅ 测试环境部署成功！服务运行正常"
              
              # 更新其他微服务
              DOCKER_IMAGE=${IMAGE_TAG} docker compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.image.yml up -d --no-deps bazi-analyzer
              sleep 10
              DOCKER_IMAGE=${IMAGE_TAG} docker compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.image.yml up -d --no-deps fortune-analyzer
              sleep 10
              DOCKER_IMAGE=${IMAGE_TAG} docker compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.image.yml up -d --no-deps rule-service
              
              echo "✅ 所有服务更新完成"
            else
              echo "❌ 测试环境部署失败！服务未响应"
              echo "📋 查看服务日志："
              docker compose -f docker-compose.yml -f docker-compose.prod.yml logs --tail=100 web
              echo "📋 查看容器状态："
              docker compose -f docker-compose.yml -f docker-compose.prod.yml ps
              exit 1
            fi
            
            # 清理未使用的镜像
            echo "🧹 清理旧镜像..."
            docker image prune -f
            
            echo "🎉 测试环境部署完成！"
          EOF
      
      # 5. 显示部署信息
      - name: 📊 Deployment summary
        if: success()
        run: |
          echo "=========================================="
          echo "🎉 测试环境部署成功！"
          echo "=========================================="
          echo "环境：测试环境 (Test)"
          echo "服务器：${{ secrets.TEST_SERVER_HOST }}"
          echo "分支：master"
          echo "提交：${{ github.sha }}"
          echo "提交者：${{ github.actor }}"
          echo "镜像：ghcr.io/${{ github.repository }}/hifate-bazi:master"
          echo "访问：http://${{ secrets.TEST_SERVER_HOST }}:8001"
          echo "=========================================="
