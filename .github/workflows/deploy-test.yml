# HiFate-bazi 测试环境自动部署
# 触发条件：推送到 master 分支（测试环境：123.57.216.15）
# 功能：从阿里云容器镜像服务 (ACR) 拉取镜像并部署到测试服务器
# 
# 标准流程：
#   1. build-and-push.yml 构建镜像并推送到 ACR
#   2. 本 workflow 从 ACR 拉取镜像
#   3. 部署到测试服务器（零停机滚动更新）

name: 🧪 Deploy to Test Environment

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  deploy:
    name: 部署到测试服务器
    runs-on: ubuntu-latest
    needs: []  # 可以添加依赖 build-and-push job，但为了灵活性暂时不依赖
    
    steps:
      # 1. 检出代码
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      # 2. 设置 SSH
      - name: 🔑 Setup SSH
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.TEST_SSH_PRIVATE_KEY }}
          
      # 3. 添加服务器到已知主机
      - name: 📝 Add server to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.TEST_SERVER_HOST }} >> ~/.ssh/known_hosts
      
      # 4. 部署到测试服务器
      - name: 🚀 Deploy to test server
        run: |
          ssh -o ServerAliveInterval=60 -o ServerAliveCountMax=3 -o ConnectTimeout=30 ${{ secrets.TEST_SERVER_USER }}@${{ secrets.TEST_SERVER_HOST }} << 'EOF'
            set -e
            
            echo "🔄 开始部署到测试环境..."
            
            # 进入项目目录
            cd /opt/HiFate-bazi
            
            # 拉取最新代码（使用 Gitee，国内服务器访问更快）
            echo "📥 拉取最新代码..."
            # 如果 remote 是 GitHub，添加 Gitee 作为备用
            if ! git remote | grep -q gitee; then
              git remote add gitee https://gitee.com/zhoudengtang/hifate-prod.git || true
            fi
            # 优先从 Gitee 拉取，失败则从 GitHub
            git fetch gitee master 2>/dev/null && git checkout master && git reset --hard gitee/master || {
              git fetch origin master 2>/dev/null && git checkout master && git reset --hard origin/master || {
                echo "⚠️  Git 拉取失败，使用现有代码继续部署"
                git checkout master 2>/dev/null || true
              }
            }
            
            # 登录到阿里云容器镜像服务
            echo "🔐 登录到阿里云容器镜像服务..."
            echo "${{ secrets.ACR_PASSWORD }}" | docker login ${{ secrets.ACR_REGISTRY }} -u ${{ secrets.ACR_USERNAME }} --password-stdin || {
              echo "❌ 无法登录到 ACR"
              exit 1
            }
            
            # 拉取最新镜像
            echo "📥 拉取最新镜像..."
            
            # 检查必要的 secrets
            if [ -z "${{ secrets.ACR_REGISTRY }}" ] || [ -z "${{ secrets.ACR_NAMESPACE }}" ]; then
              echo "❌ ACR_REGISTRY 或 ACR_NAMESPACE 未配置，跳过镜像拉取"
              echo "⚠️  将使用本地构建的镜像或现有镜像"
              IMAGE_TAG="hifate-bazi:master"
            else
              IMAGE_TAG="${{ secrets.ACR_REGISTRY }}/${{ secrets.ACR_NAMESPACE }}/hifate-bazi:master"
              
              # 验证镜像标签格式
              if [[ ! "${IMAGE_TAG}" =~ ^[a-zA-Z0-9._/-]+:[a-zA-Z0-9._-]+$ ]]; then
                echo "❌ 镜像标签格式无效: ${IMAGE_TAG}"
                echo "⚠️  将使用本地构建的镜像或现有镜像"
                IMAGE_TAG="hifate-bazi:master"
              else
                # 拉取镜像（不使用 timeout，让 SSH 保活机制处理）
                if docker pull ${IMAGE_TAG} 2>&1; then
                  echo "✅ 镜像拉取成功: ${IMAGE_TAG}"
                else
                  echo "⚠️  拉取 master 标签失败，尝试 latest 标签"
                  IMAGE_TAG="${{ secrets.ACR_REGISTRY }}/${{ secrets.ACR_NAMESPACE }}/hifate-bazi:latest"
                  if docker pull ${IMAGE_TAG} 2>&1; then
                    echo "✅ 镜像拉取成功: ${IMAGE_TAG}"
                  else
                    echo "⚠️  无法拉取远程镜像，将使用本地构建"
                    IMAGE_TAG="hifate-bazi:master"
                  fi
                fi
              fi
            fi
            
            echo "📦 使用镜像: ${IMAGE_TAG}"
            
            # 停止并删除旧容器（避免名称冲突）
            echo "🛑 停止旧容器..."
            docker compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.image.yml stop web 2>/dev/null || true
            docker compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.image.yml rm -f web 2>/dev/null || true
            
            # 清理可能存在的同名容器（通过容器名称查找）
            CONTAINER_IDS=$(docker ps -a --filter "name=hifate-web" --format "{{.ID}}" 2>/dev/null || true)
            if [ -n "$CONTAINER_IDS" ]; then
              echo "🧹 清理同名容器..."
              echo "$CONTAINER_IDS" | xargs -r docker rm -f 2>/dev/null || true
            fi
            
            # 滚动更新（零停机部署）
            echo "🔄 滚动更新服务..."
            DOCKER_IMAGE=${IMAGE_TAG} docker compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.image.yml up -d --force-recreate --no-deps web
            
            # 等待新容器启动
            echo "⏳ 等待新容器启动..."
            sleep 20
            
            # 健康检查（重试机制）
            echo "🏥 健康检查..."
            MAX_RETRIES=5
            RETRY_COUNT=0
            HEALTH_CHECK_PASSED=false
            
            while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
              if curl -f -s http://localhost:8001/health > /dev/null 2>&1; then
                HEALTH_CHECK_PASSED=true
                break
              fi
              RETRY_COUNT=$((RETRY_COUNT + 1))
              echo "⏳ 健康检查失败，重试 $RETRY_COUNT/$MAX_RETRIES..."
              sleep 5
            done
            
            if [ "$HEALTH_CHECK_PASSED" = true ]; then
              echo "✅ 测试环境部署成功！服务运行正常"
              
              # 更新其他微服务
              DOCKER_IMAGE=${IMAGE_TAG} docker compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.image.yml up -d --no-deps bazi-analyzer
              sleep 10
              DOCKER_IMAGE=${IMAGE_TAG} docker compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.image.yml up -d --no-deps fortune-analyzer
              sleep 10
              DOCKER_IMAGE=${IMAGE_TAG} docker compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.image.yml up -d --no-deps rule-service
              
              echo "✅ 所有服务更新完成"
            else
              echo "❌ 测试环境部署失败！服务未响应"
              echo "📋 查看服务日志："
              docker compose -f docker-compose.yml -f docker-compose.prod.yml logs --tail=100 web
              echo "📋 查看容器状态："
              docker compose -f docker-compose.yml -f docker-compose.prod.yml ps
              exit 1
            fi
            
            # 清理未使用的镜像
            echo "🧹 清理旧镜像..."
            docker image prune -f
            
            echo "🎉 测试环境部署完成！"
          EOF
      
      # 5. 显示部署信息
      - name: 📊 Deployment summary
        if: success()
        run: |
          echo "=========================================="
          echo "🎉 测试环境部署成功！"
          echo "=========================================="
          echo "环境：测试环境 (Test)"
          echo "服务器：${{ secrets.TEST_SERVER_HOST }}"
          echo "分支：master"
          echo "提交：${{ github.sha }}"
          echo "提交者：${{ github.actor }}"
          echo "镜像：${{ secrets.ACR_REGISTRY }}/${{ secrets.ACR_NAMESPACE }}/hifate-bazi:master"
          echo "访问：http://${{ secrets.TEST_SERVER_HOST }}:8001"
          echo "=========================================="
