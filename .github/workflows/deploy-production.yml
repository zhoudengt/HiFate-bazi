# HiFate-bazi 生产环境自动部署
# 触发条件：推送到 master 分支

name: 🚀 Deploy to Production

on:
  workflow_dispatch:  # 仅支持手动触发（生产环境必须手动部署）

jobs:
  deploy:
    name: 部署到生产服务器
    runs-on: ubuntu-latest
    
    steps:
      # 1. 检出代码
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        
      # 2. 设置 SSH
      - name: 🔑 Setup SSH
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.PROD_SSH_PRIVATE_KEY }}
          
      # 3. 添加服务器到已知主机
      - name: 📝 Add server to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.PROD_SERVER_HOST }} >> ~/.ssh/known_hosts
          
      # 4. 备份数据库（生产环境）
      - name: 💾 Backup database
        run: |
          ssh ${{ secrets.PROD_SERVER_USER }}@${{ secrets.PROD_SERVER_HOST }} << 'EOF'
            set -e
            
            echo "💾 备份生产数据库..."
            cd /opt/HiFate-bazi
            
            # 创建备份目录
            mkdir -p backups
            
            # 备份数据库（使用环境变量中的数据库名）
            BACKUP_FILE="backups/mysql_backup_$(date +%Y%m%d_%H%M%S).sql"
            DB_NAME="${MYSQL_DATABASE:-hifate_bazi}"
            docker-compose exec -T mysql mysqldump -u root -p${MYSQL_ROOT_PASSWORD} ${DB_NAME} > $BACKUP_FILE
            
            echo "✅ 数据库备份完成：$BACKUP_FILE"
            
            # 保留最近7天的备份
            find backups/ -name "mysql_backup_*.sql" -mtime +7 -delete
          EOF
          
      # 5. 等待镜像构建完成（如果 build-and-push workflow 正在运行）
      - name: ⏳ Wait for image build
        run: |
          echo "⏳ 等待镜像构建完成..."
          # 等待最多 10 分钟，每 30 秒检查一次
          MAX_WAIT=600
          WAIT_TIME=0
          while [ $WAIT_TIME -lt $MAX_WAIT ]; do
            if docker pull ghcr.io/${{ github.repository }}/hifate-bazi:master 2>/dev/null; then
              echo "✅ 镜像已可用"
              break
            fi
            echo "⏳ 镜像尚未就绪，等待中... ($WAIT_TIME/$MAX_WAIT 秒)"
            sleep 30
            WAIT_TIME=$((WAIT_TIME + 30))
          done
      
      # 6. 部署到生产服务器
      - name: 🚀 Deploy to production server
        run: |
          ssh ${{ secrets.PROD_SERVER_USER }}@${{ secrets.PROD_SERVER_HOST }} << 'EOF'
            set -e
            
            echo "🔄 开始部署到生产环境..."
            
            # 进入项目目录
            cd /opt/HiFate-bazi
            
            # 拉取最新代码（用于 docker-compose 配置）
            echo "📥 拉取最新代码..."
            git fetch origin
            git checkout master
            git pull origin master
            
            # 登录到 GitHub Container Registry
            echo "🔐 登录到 GitHub Container Registry..."
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin || {
              echo "⚠️  使用 GITHUB_TOKEN 登录失败，尝试使用 Personal Access Token"
              if [ -n "${{ secrets.GHCR_TOKEN }}" ]; then
                echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
              else
                echo "❌ 无法登录到 GitHub Container Registry"
                echo "请配置 GHCR_TOKEN secret 或确保 GITHUB_TOKEN 有 packages:write 权限"
                exit 1
              fi
            }
            
            # 拉取最新镜像（从 GitHub Container Registry）
            echo "📥 拉取最新镜像..."
            IMAGE_TAG="ghcr.io/${{ github.repository }}/hifate-bazi:master"
            docker pull ${IMAGE_TAG} || {
              echo "⚠️  拉取 master 标签失败，尝试 latest 标签"
              IMAGE_TAG="ghcr.io/${{ github.repository }}/hifate-bazi:latest"
              docker pull ${IMAGE_TAG}
            }
            
            echo "✅ 镜像拉取成功: ${IMAGE_TAG}"
            
            # 只清理已停止的容器（零停机部署）
            echo "🧹 清理已停止的容器..."
            # 只删除已停止的容器，不影响正在运行的容器
            docker-compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.image.yml rm -f web 2>/dev/null || true
            
            # 清理可能存在的同名僵尸容器（已停止但未删除的）
            CONTAINER_IDS=$(docker ps -a --filter "name=hifate-web" --filter "status=exited" --format "{{.ID}}" 2>/dev/null || true)
            if [ -n "$CONTAINER_IDS" ]; then
              echo "🧹 清理僵尸容器..."
              echo "$CONTAINER_IDS" | xargs -r docker rm -f 2>/dev/null || true
            fi
            
            # 滚动更新（零停机部署）- Docker Compose 会自动处理
            # up 命令会先启动新容器，健康检查通过后再停止旧容器
            echo "🔄 滚动更新服务（零停机）..."
            DOCKER_IMAGE=${IMAGE_TAG} docker-compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.image.yml up -d --no-deps web
            
            # 等待新容器启动
            echo "⏳ 等待新容器启动..."
            sleep 20
            
            # 健康检查（重试机制）
            echo "🏥 健康检查..."
            MAX_RETRIES=5
            RETRY_COUNT=0
            HEALTH_CHECK_PASSED=false
            
            while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
              if curl -f -s http://localhost:8001/health > /dev/null 2>&1; then
                HEALTH_CHECK_PASSED=true
                break
              fi
              RETRY_COUNT=$((RETRY_COUNT + 1))
              echo "⏳ 健康检查失败，重试 $RETRY_COUNT/$MAX_RETRIES..."
              sleep 5
            done
            
            if [ "$HEALTH_CHECK_PASSED" = true ]; then
              echo "✅ 新版本运行正常，更新其他服务..."
              
              # 更新其他微服务（使用拉取的镜像）
              DOCKER_IMAGE=${IMAGE_TAG} docker-compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.image.yml up -d --no-deps bazi-analyzer
              sleep 10
              DOCKER_IMAGE=${IMAGE_TAG} docker-compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.image.yml up -d --no-deps fortune-analyzer
              sleep 10
              DOCKER_IMAGE=${IMAGE_TAG} docker-compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.image.yml up -d --no-deps rule-service
              
              echo "✅ 所有服务更新完成"
            else
              echo "❌ 新版本健康检查失败，回滚..."
              
              # 回滚到上一个版本（拉取上一个 commit 的镜像）
              echo "🔄 回滚到上一个版本..."
              PREVIOUS_SHA=$(git rev-parse HEAD^)
              PREVIOUS_IMAGE="ghcr.io/${{ github.repository }}/hifate-bazi:${PREVIOUS_SHA:0:7}"
              
              # 清理已停止的容器（回滚时也需要零停机）
              echo "🧹 清理已停止的容器..."
              docker-compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.image.yml rm -f web 2>/dev/null || true
              CONTAINER_IDS=$(docker ps -a --filter "name=hifate-web" --filter "status=exited" --format "{{.ID}}" 2>/dev/null || true)
              if [ -n "$CONTAINER_IDS" ]; then
                echo "$CONTAINER_IDS" | xargs -r docker rm -f 2>/dev/null || true
              fi
              
              # 尝试拉取上一个版本的镜像
              if docker pull ${PREVIOUS_IMAGE} 2>/dev/null; then
                echo "✅ 找到上一个版本的镜像: ${PREVIOUS_IMAGE}"
                DOCKER_IMAGE=${PREVIOUS_IMAGE} docker-compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.image.yml up -d --no-deps web
              else
                echo "⚠️  未找到上一个版本的镜像，使用 latest 标签"
                DOCKER_IMAGE="ghcr.io/${{ github.repository }}/hifate-bazi:latest" docker-compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.image.yml up -d --no-deps web
              fi
              
              echo "❌ 已回滚到上一个版本"
              exit 1
            fi
            
            # 清理未使用的镜像
            echo "🧹 清理旧镜像..."
            docker image prune -f
            
            # 最终健康检查（重试机制）
            echo "🏥 最终健康检查..."
            sleep 10
            MAX_FINAL_RETRIES=3
            FINAL_RETRY_COUNT=0
            FINAL_HEALTH_CHECK_PASSED=false
            
            while [ $FINAL_RETRY_COUNT -lt $MAX_FINAL_RETRIES ]; do
              if curl -f -s http://localhost:8001/health > /dev/null 2>&1; then
                FINAL_HEALTH_CHECK_PASSED=true
                break
              fi
              FINAL_RETRY_COUNT=$((FINAL_RETRY_COUNT + 1))
              echo "⏳ 最终健康检查失败，重试 $FINAL_RETRY_COUNT/$MAX_FINAL_RETRIES..."
              sleep 5
            done
            
            if [ "$FINAL_HEALTH_CHECK_PASSED" = true ]; then
              echo "✅ 部署成功！所有服务运行正常"
            else
              echo "❌ 部署后健康检查失败"
              echo "📋 查看服务日志："
              docker-compose -f docker-compose.yml -f docker-compose.prod.yml logs --tail=100 web
              echo "📋 查看容器状态："
              docker-compose -f docker-compose.yml -f docker-compose.prod.yml ps
              exit 1
            fi
            
            echo "🎉 生产环境部署完成！"
          EOF
          
      # 6. 通知（可选，需要配置 Slack/钉钉等）
      - name: 📢 Send notification
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "✅ 生产环境部署成功"
          else
            echo "❌ 生产环境部署失败"
          fi
          
      # 7. 显示部署信息
      - name: 📊 Deployment summary
        if: success()
        run: |
          echo "=========================================="
          echo "🎉 部署成功！"
          echo "=========================================="
          echo "环境：生产环境 (Production)"
          echo "分支：master"
          echo "提交：${{ github.sha }}"
          echo "提交者：${{ github.actor }}"
          echo "访问：https://${{ secrets.PROD_SERVER_HOST }}"
          echo "=========================================="
          
      # 8. 创建发布标签
      - name: 🏷️ Create release tag
        if: success()
        run: |
          TAG_NAME="v$(date +%Y%m%d_%H%M%S)"
          git tag $TAG_NAME
          git push origin $TAG_NAME
          echo "✅ 已创建发布标签：$TAG_NAME"

