---
description: 快捷命令触发器（用户说特定关键词时自动执行对应操作）
alwaysApply: true
---

# 快捷命令触发器

> 用户说以下关键词时，**直接执行**对应命令，无需再确认（除非有未提交代码等阻碍）。

---

## 🚀 部署相关

| 用户说 | 执行命令 | 说明 |
|--------|---------|------|
| **芝麻开门** | `bash deploy/scripts/gated_deploy.sh` | 标准门控发布 |
| 芝麻开门 dry-run | `bash deploy/scripts/gated_deploy.sh --dry-run` | 只在 Node2 验证 |
| 芝麻开门 紧急 | `bash deploy/scripts/gated_deploy.sh --skip-node2` | 跳过 Node2（慎用） |
| 芝麻开门 回滚 | `bash deploy/scripts/gated_deploy.sh --rollback` | 回滚到上一版本 |

详见 `@gated-deploy.mdc`

---

## 🧪 测试相关

| 用户说 | 执行命令 | 说明 |
|--------|---------|------|
| **测试** | `python3 scripts/evaluation/api_regression_test.py --env production --category basic --category stream --category payment --parallel` | 全接口回归测试 |
| 本地测试 | 替换 `--env production` 为 `--env local` | 本地环境测试 |

---

## 🔄 热更新相关

| 用户说 | 执行命令 | 说明 |
|--------|---------|------|
| 热更新 | `python3 scripts/ai/auto_hot_reload.py --trigger` | 触发热更新 |
| 验证热更新 | `python3 scripts/ai/auto_hot_reload.py --verify` | 验证热更新状态 |

---

## 📋 提交前检查

| 用户说 | 执行操作 | 说明 |
|--------|---------|------|
| 提交前检查 | 加载 `@dev-checklist.mdc` | 提交前完整检查清单 |

---

## 注意事项

1. **芝麻开门**系列命令会自动完成 Git 检查，如有未提交代码会提示
2. 测试命令覆盖 23 个接口（基本5 + 流式13 + 支付5）
3. 热更新命令适用于本地开发环境
4. 生产环境部署必须通过门控发布，禁止直接操作服务器
