# 🧪 HiFate-bazi 测试规范

## 一、测试金字塔

```
            ┌────────────────┐
            │   E2E 测试     │  10%  - 端到端用户场景
            │  (Selenium)    │
            ├────────────────┤
            │   集成测试     │  20%  - 服务间交互
            │  (pytest)      │
            ├────────────────┤
            │   单元测试     │  70%  - 函数/类测试
            │  (pytest)      │
            └────────────────┘

目标覆盖率：> 80%
```

---

## 二、测试目录结构

```
tests/
├── conftest.py              # pytest 全局配置
├── fixtures/                # 测试夹具
│   ├── __init__.py
│   └── sample_data.py       # 测试数据
├── unit/                    # 单元测试
│   ├── __init__.py
│   ├── test_bazi_service.py
│   └── test_calendar_service.py
├── integration/             # 集成测试
│   ├── __init__.py
│   └── test_api_bazi.py
├── api/                     # API 测试
│   ├── __init__.py
│   └── test_calendar_api.py
├── features/                # 功能测试
│   ├── __init__.py
│   └── test_payment.py
└── scripts/                 # 测试脚本
    └── test_payment_complete.sh
```

---

## 三、测试文件命名规范

| 类型 | 命名格式 | 示例 |
|------|----------|------|
| 单元测试 | `test_{module}.py` | `test_calendar_service.py` |
| 集成测试 | `test_{feature}_integration.py` | `test_api_bazi.py` |
| API 测试 | `test_{api_name}_api.py` | `test_calendar_api.py` |
| 功能测试 | `test_{feature}.py` | `test_payment.py` |

---

## 四、测试代码规范

### 4.1 测试类结构

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
{模块名} 测试

测试范围：
- 功能点1
- 功能点2
"""

import pytest
from unittest.mock import Mock, patch, MagicMock
from server.services.calendar_api_service import CalendarAPIService


class TestCalendarAPIService:
    """CalendarAPIService 测试类"""
    
    # ==================== 测试前置/后置 ====================
    
    def setup_method(self):
        """每个测试方法前执行"""
        self.service = CalendarAPIService()
    
    def teardown_method(self):
        """每个测试方法后执行"""
        pass
    
    @pytest.fixture(autouse=True)
    def setup_fixtures(self):
        """自动使用的 fixture"""
        pass
    
    # ==================== 单元测试 ====================
    
    def test_get_calendar_with_valid_date(self):
        """测试：有效日期查询"""
        # Given: 准备测试数据
        date = "2025-12-11"
        
        # When: 执行被测方法
        result = self.service.get_calendar(date)
        
        # Then: 验证结果
        assert result["success"] == True
        assert result["lunar_date"] is not None
        assert "ganzhi" in result
    
    def test_get_calendar_with_invalid_date(self):
        """测试：无效日期格式"""
        # Given
        invalid_date = "invalid-date"
        
        # When
        result = self.service.get_calendar(invalid_date)
        
        # Then
        assert result["success"] == False
        assert "error" in result
    
    # ==================== 边界测试 ====================
    
    def test_get_calendar_with_none_date(self):
        """测试：日期为空（使用今天）"""
        # When
        result = self.service.get_calendar(None)
        
        # Then
        assert result["success"] == True
    
    def test_get_calendar_with_empty_string(self):
        """测试：空字符串"""
        # When
        result = self.service.get_calendar("")
        
        # Then
        # 根据业务逻辑，空字符串可能使用今天或报错
        assert result is not None
    
    # ==================== Mock 测试 ====================
    
    @patch('server.services.calendar_api_service.requests.get')
    def test_api_call_failure(self, mock_get):
        """测试：第三方 API 调用失败"""
        # Given: Mock API 返回错误
        mock_get.side_effect = Exception("API Error")
        
        # When: 调用服务
        result = self.service.get_calendar("2025-12-11")
        
        # Then: 应该使用本地计算
        assert result["success"] == True
        assert result["provider"] == "local"
```

### 4.2 测试方法命名

```python
# 命名格式：test_{被测方法}_{场景}_{预期结果}

# ✅ 正确示例
def test_get_calendar_with_valid_date_returns_success(self):
    pass

def test_get_calendar_with_invalid_date_returns_error(self):
    pass

def test_calculate_bazi_with_leap_month_handles_correctly(self):
    pass

# ❌ 错误示例
def test1(self):  # 不明确
    pass

def testGetCalendar(self):  # 应该用下划线
    pass
```

### 4.3 Given-When-Then 模式

```python
def test_get_calendar_with_valid_date(self):
    """测试：有效日期查询返回完整数据"""
    
    # Given: 准备测试数据和依赖
    date = "2025-12-11"
    expected_lunar = "农历十月廿二"
    
    # When: 执行被测代码
    result = self.service.get_calendar(date)
    
    # Then: 验证结果
    assert result["success"] == True
    assert result["lunar_date"] == expected_lunar
    assert result["ganzhi"]["year"] == "乙巳"
```

---

## 五、Pytest 配置

### 5.1 pytest.ini 配置

**文件位置：** 项目根目录 `pytest.ini`

```ini
[pytest]
# 测试目录
testpaths = tests

# 测试文件匹配
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 默认参数
addopts = -v --tb=short --strict-markers

# 标记定义
markers =
    slow: 标记为慢速测试
    integration: 集成测试
    api: API 测试
    unit: 单元测试

# 忽略警告
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning

# 异步测试支持
asyncio_mode = auto
```

### 5.2 conftest.py 配置

**文件位置：** `tests/conftest.py`

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Pytest 全局配置
"""

import pytest
import sys
import os
from typing import Generator

# 添加项目根目录到路径
project_root = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, project_root)


# ==================== Fixtures ====================

@pytest.fixture(scope="session")
def app():
    """创建 FastAPI 应用实例"""
    from server.main import app
    return app


@pytest.fixture(scope="session")
def client(app):
    """创建测试客户端"""
    from fastapi.testclient import TestClient
    return TestClient(app)


@pytest.fixture(scope="function")
def sample_date():
    """示例日期"""
    return "2025-12-11"


@pytest.fixture(scope="function")
def sample_bazi_request():
    """示例八字请求"""
    return {
        "solar_date": "1990-01-15",
        "solar_time": "12:00",
        "gender": "male"
    }


@pytest.fixture(scope="function")
def mock_db_connection():
    """Mock 数据库连接"""
    from unittest.mock import MagicMock
    mock_conn = MagicMock()
    yield mock_conn
    mock_conn.close()


# ==================== Hooks ====================

def pytest_configure(config):
    """pytest 配置钩子"""
    # 添加自定义标记
    config.addinivalue_line("markers", "slow: 慢速测试")
    config.addinivalue_line("markers", "integration: 集成测试")


def pytest_collection_modifyitems(config, items):
    """修改测试收集"""
    # 为集成测试添加标记
    for item in items:
        if "integration" in item.nodeid:
            item.add_marker(pytest.mark.integration)
```

---

## 六、测试数据管理

### 6.1 测试数据文件

**文件位置：** `tests/fixtures/sample_data.py`

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
测试数据
"""

# 有效日期数据
VALID_DATES = [
    "2025-12-11",
    "2024-01-01",
    "2000-06-15",
]

# 无效日期数据
INVALID_DATES = [
    "invalid",
    "2025-13-01",  # 月份无效
    "2025-02-30",  # 日期无效
]

# 八字请求数据
BAZI_REQUESTS = [
    {
        "solar_date": "1990-01-15",
        "solar_time": "12:00",
        "gender": "male",
        "expected": {
            "year_ganzhi": "己巳",
            "month_ganzhi": "丁丑"
        }
    },
    {
        "solar_date": "2000-06-15",
        "solar_time": "08:30",
        "gender": "female",
        "expected": {
            "year_ganzhi": "庚辰"
        }
    }
]

# 万年历期望数据
CALENDAR_EXPECTED = {
    "2025-12-11": {
        "lunar_date": "农历十月廿二",
        "ganzhi": {
            "year": "乙巳",
            "month": "戊子",
            "day": "甲寅"
        },
        "shengxiao": "蛇",
        "xingzuo": "射手"
    }
}
```

### 6.2 参数化测试

```python
import pytest
from tests.fixtures.sample_data import VALID_DATES, INVALID_DATES, CALENDAR_EXPECTED

class TestCalendarService:
    
    @pytest.mark.parametrize("date", VALID_DATES)
    def test_get_calendar_with_valid_dates(self, date):
        """测试多个有效日期"""
        result = self.service.get_calendar(date)
        assert result["success"] == True
    
    @pytest.mark.parametrize("date", INVALID_DATES)
    def test_get_calendar_with_invalid_dates(self, date):
        """测试多个无效日期"""
        result = self.service.get_calendar(date)
        assert result["success"] == False
    
    @pytest.mark.parametrize("date,expected", [
        ("2025-12-11", CALENDAR_EXPECTED["2025-12-11"]),
    ])
    def test_get_calendar_with_expected_data(self, date, expected):
        """测试预期数据匹配"""
        result = self.service.get_calendar(date)
        assert result["lunar_date"] == expected["lunar_date"]
        assert result["shengxiao"] == expected["shengxiao"]
```

---

## 七、运行测试

### 7.1 常用命令

```bash
# 运行所有测试
pytest

# 运行单元测试
pytest tests/unit/ -v

# 运行特定文件
pytest tests/unit/test_calendar_service.py -v

# 运行特定测试方法
pytest tests/unit/test_calendar_service.py::TestCalendarService::test_get_calendar_with_valid_date -v

# 运行带标记的测试
pytest -m "unit" -v
pytest -m "not slow" -v

# 查看覆盖率
pytest --cov=server --cov-report=html

# 并行运行
pytest -n auto

# 失败后重试
pytest --lf  # 只运行上次失败的测试
pytest --ff  # 先运行失败的测试
```

### 7.2 CI/CD 集成

```yaml
# .github/workflows/test.yml
name: Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest pytest-cov pytest-asyncio
      
      - name: Run tests
        run: |
          pytest tests/ -v --cov=server --cov-report=xml
      
      - name: Upload coverage
        uses: codecov/codecov-action@v3
```

---

## 八、测试检查清单

每个功能必须包含以下测试：

### 8.1 单元测试检查清单

- [ ] 正常流程测试（happy path）
- [ ] 边界条件测试（空值、极端值）
- [ ] 异常处理测试（错误输入、异常情况）
- [ ] Mock 外部依赖

### 8.2 API 测试检查清单

- [ ] 成功响应测试（200）
- [ ] 参数验证测试（400）
- [ ] 未授权测试（401，如适用）
- [ ] 资源不存在测试（404，如适用）
- [ ] 服务器错误测试（500）

### 8.3 集成测试检查清单

- [ ] 服务间调用测试
- [ ] 数据库交互测试
- [ ] 缓存交互测试

---

## 九、Mock 使用指南

### 9.1 Mock 外部 API

```python
from unittest.mock import patch, Mock

@patch('requests.get')
def test_external_api_call(self, mock_get):
    """Mock 外部 API 调用"""
    # 设置 Mock 返回值
    mock_response = Mock()
    mock_response.status_code = 200
    mock_response.json.return_value = {"data": "test"}
    mock_get.return_value = mock_response
    
    # 执行测试
    result = self.service.call_external_api()
    
    # 验证
    assert result["data"] == "test"
    mock_get.assert_called_once()
```

### 9.2 Mock 数据库

```python
@patch('server.config.mysql_config.get_mysql_connection')
def test_database_query(self, mock_conn):
    """Mock 数据库查询"""
    # 设置 Mock
    mock_cursor = Mock()
    mock_cursor.fetchall.return_value = [{"id": 1, "name": "test"}]
    mock_conn.return_value.__enter__.return_value.cursor.return_value.__enter__.return_value = mock_cursor
    
    # 执行测试
    result = self.service.query_data()
    
    # 验证
    assert len(result) == 1
```

---

## 十、覆盖率要求

| 模块 | 最低覆盖率 | 说明 |
|------|-----------|------|
| `server/services/` | 80% | 业务逻辑核心 |
| `server/api/` | 70% | API 路由 |
| `src/analyzers/` | 80% | 分析器 |
| `src/bazi_core/` | 90% | 核心计算 |
| 整体项目 | 70% | 全局目标 |

```bash
# 生成覆盖率报告
pytest --cov=server --cov=src --cov-report=html

# 查看报告
open htmlcov/index.html
```

---

## 十一、流式接口性能测试工具（Stream Profiler）

### 11.1 工具名称与路径

| 项目 | 说明 |
|------|------|
| **正式名称** | 流式接口各环节耗时测试工具（Stream Profiler） |
| **代码路径** | `tools/stream_profiler/` |
| **规范引用** | 本文档 十一、`standards/llm-development.md` 流式接口优化规范、`tools/stream_profiler/README.md` |

用于后续各流式接口的性能测试与优化验证。工具与 server/api 解耦，仅通过 HTTP 调用流式接口并解析 SSE，不修改、不依赖 server/core。

### 11.2 基本用法

在项目根目录执行：

```bash
# 测所有配置的流式接口，各 1 次
python3 -m tools.stream_profiler.cli --base-url http://localhost:8001

# 只测指定接口，多轮取平均
python3 -m tools.stream_profiler.cli --base-url http://localhost:8001 --endpoint /api/v1/bazi/wuxing-proportion/stream --runs 3 --output report.md

# 输出 JSON
python3 -m tools.stream_profiler.cli --base-url http://localhost:8001 --format json --output report.json
```

### 11.3 指标说明（按阶段理解）

报告按「编排取数 → 大模型首字 → 大模型流式反馈」等阶段展示，并带结论解读：

| 阶段 | 报告中的指标名 | 含义 |
|------|----------------|------|
| 从统一编排获取数据 | 编排取数 | 从请求到首条结构化数据的时间（从 BaziDataOrchestrator 取数并返回首包） |
| 数据交给大模型 → 大模型开始输出 | 大模型首字延迟 | 从首包(data)到首条流式内容(progress)的时间（含交 LLM、LLM 计算、首字返回） |
| 大模型逐字/逐段反馈 | 大模型流式输出 | 从首条 progress 到 complete 的时间 |
| 整次请求结束 | 总耗时 | 从请求到 complete 的时间 |

**结论**：报告末尾「结论」小节会写各接口的编排取数、大模型首字延迟、大模型流式输出、总耗时；若大模型首字延迟和总耗时都很短，多为缓存命中；若大模型首字延迟较长，多为冷请求。详细说明与参数见 `tools/stream_profiler/README.md`。

### 11.4 文档同步约定

**工具修改时，须同步更新**：

- 本小节（十一）：若工具名称、路径、命令或指标变更
- `tools/stream_profiler/README.md`：若使用方式、指标、参数变更
- `standards/llm-development.md` 流式接口优化规范：若引用到本工具的命令或能力，须一并更新
