# 📁 HiFate-bazi 目录结构标准

## 一、目录结构概览

```
HiFate-bazi/
├── core/                         # 🧮 核心计算层（零外部依赖）
│   ├── calculators/             # 八字计算器
│   ├── analyzers/               # 分析器
│   ├── config/                  # 八字配置数据
│   ├── data/                    # 数据常量
│   └── models/                  # 领域模型
│
├── shared/                       # 🔗 共享层（跨层共享）
│   ├── config/                  # 统一配置（数据库、Redis等）
│   ├── clients/                 # gRPC 客户端
│   └── utils/                   # 通用工具
│
├── server/                       # 🌐 应用层（FastAPI 主入口）
│   ├── api/                     # API 路由层
│   │   ├── v1/                 # V1 版本 API
│   │   ├── v2/                 # V2 版本 API
│   │   └── grpc_gateway.py     # gRPC-Web 网关
│   ├── orchestrators/           # 编排服务（数据聚合）
│   ├── services/                # 业务服务层
│   ├── engines/                 # 规则引擎
│   ├── config/                  # 应用配置
│   ├── db/                      # 数据库连接和迁移
│   ├── utils/                   # 工具函数
│   └── main.py                  # 应用入口
│
├── services/                     # 🔧 微服务层（独立部署）
│   ├── bazi_core/              # 八字核心计算服务 (9001)
│   ├── bazi_fortune/           # 运势计算服务 (9002)
│   ├── bazi_analyzer/          # 八字分析服务 (9003)
│   ├── bazi_rule/              # 规则匹配服务 (9004)
│   ├── fortune_analysis/       # 运势分析服务 (9005)
│   ├── payment_service/        # 支付服务 (9006)
│   ├── fortune_rule/           # 运势规则服务 (9007)
│   ├── intent_service/         # 意图识别服务 (9008)
│   ├── prompt_optimizer/       # 提示优化服务 (9009)
│   └── desk_fengshui/          # 办公桌风水服务 (9010)
│
├── local_frontend/               # 🎨 前端代码
│   ├── css/                     # 样式文件
│   ├── js/                      # JavaScript 文件
│   └── *.html                   # HTML 页面
│
├── tests/                        # 🧪 测试代码
│   ├── unit/                    # 单元测试
│   ├── integration/             # 集成测试
│   ├── api/                     # API 测试
│   └── features/                # 功能测试
│
├── scripts/                      # 📜 脚本工具
│   ├── migration/               # 数据迁移脚本
│   ├── docker/                  # Docker 相关脚本
│   ├── ai/                      # AI 开发工具
│   └── tests/                   # 测试脚本
│
├── tools/                        # 🔧 独立工具（与 server 解耦，零业务依赖）
│   └── stream_profiler/         # 流式接口各环节耗时测试工具（Stream Profiler）
│
├── standards/                    # 📋 开发标准
│   ├── 01_目录结构标准.md
│   ├── 02_命名规范.md
│   ├── 03_开发者手册.md
│   └── templates/               # 代码模板
│
├── proto/                        # 📡 gRPC 协议定义
│   └── *.proto                  # Protocol Buffers 文件
│
├── deploy/                       # 🚀 部署配置
│   ├── docs/                    # 部署文档
│   └── scripts/                 # 部署脚本
│
├── region_code_table.json        # 地区码表（脚本引用）
└── docker-compose*.yml           # 🐳 Docker 编排文件
```

---

## 二、分层架构说明

### 2.1 分层依赖关系

```
┌─────────────────────────────────────────────────────────────┐
│                      API 层 (server/api/)                    │
│                  处理 HTTP/gRPC 请求                         │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                  编排层 (server/orchestrators/)              │
│              协调多服务调用、数据聚合                        │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                  服务层 (server/services/)                   │
│                  业务逻辑处理                                │
└──────────────────────────┬──────────────────────────────────┘
                           │
           ┌───────────────┼───────────────┐
           ▼               ▼               ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│   核心层        │ │   共享层        │ │   微服务层      │
│   (core/)       │ │   (shared/)     │ │   (services/)   │
│   纯计算逻辑    │ │   配置/客户端   │ │   独立服务      │
└─────────────────┘ └─────────────────┘ └─────────────────┘
```

### 2.2 依赖规则

| 层级 | 可依赖 | 禁止依赖 |
|------|--------|----------|
| `core/` | 仅标准库 | `server/`, `services/`, `shared/` |
| `shared/` | `core/` | `server/`, `services/` |
| `services/` 微服务 | `core/`, `shared/` | `server/` |
| `server/services/` | `core/`, `shared/`, 微服务客户端 | - |
| `server/api/` | `server/services/`, `server/orchestrators/` | 直接调用 `core/` |

---

## 三、目录职责说明

### 3.1 core/ - 核心计算层

**职责：** 纯业务计算逻辑，零外部依赖，可独立测试

| 子目录 | 职责 | 示例文件 |
|--------|------|----------|
| `calculators/` | 八字计算器 | `bazi_calculator.py` |
| `analyzers/` | 各类分析器 | `wangshuai_analyzer.py` |
| `config/` | 八字配置数据 | `ten_gods_config.py` |
| `data/` | 数据常量 | `constants.py`, `relations.py` |
| `models/` | 领域模型 | `bazi_model.py` |

**使用原则：**
- ✅ 纯 Python 标准库
- ✅ 可被任何层复用
- ❌ 不包含 HTTP/gRPC 代码
- ❌ 不包含数据库操作
- ❌ 不依赖第三方服务

### 3.2 shared/ - 共享层

**职责：** 跨层共享的模块

| 子目录 | 职责 | 示例文件 |
|--------|------|----------|
| `config/` | 统一配置 | `database.py`, `redis.py` |
| `clients/` | gRPC 客户端 | `bazi_core_client_grpc.py` |
| `utils/` | 通用工具 | - |

### 3.3 server/ - 应用层

**职责：** Web 服务，处理 HTTP/gRPC 请求

| 子目录 | 职责 | 示例文件 |
|--------|------|----------|
| `api/v1/` | V1 版本 REST API | `bazi.py`, `calendar_api.py` |
| `api/v2/` | V2 版本 REST API | `face_analysis.py` |
| `orchestrators/` | 数据编排服务 | `bazi_data_orchestrator.py` |
| `services/` | 业务服务 | `bazi_service.py` |
| `engines/` | 规则引擎 | `rule_engine.py` |
| `config/` | 应用配置 | `app_config.py` |
| `db/` | 数据库访问 | `mysql_connector.py` |
| `utils/` | 工具函数 | `cache.py` |

### 3.4 services/ - 微服务层

**职责：** 独立运行的 gRPC 微服务

| 服务 | 端口 | 职责 |
|------|------|------|
| `bazi_core` | 9001 | 八字核心计算 |
| `bazi_fortune` | 9002 | 运势计算 |
| `bazi_analyzer` | 9003 | 八字分析 |
| `bazi_rule` | 9004 | 规则匹配 |
| `fortune_analysis` | 9005 | 运势分析 |
| `payment_service` | 9006 | 支付服务 |
| `fortune_rule` | 9007 | 运势规则 |
| `intent_service` | 9008 | 意图识别 |
| `prompt_optimizer` | 9009 | 提示优化 |
| `desk_fengshui` | 9010 | 办公桌风水 |

**标准微服务结构：**
```
services/{service_name}/
├── __init__.py          # 模块初始化
├── grpc_server.py       # gRPC 服务端
├── main.py              # 服务入口
├── schemas.py           # 数据模型
├── service.py           # 业务逻辑（可选）
└── requirements.txt     # 独立依赖（可选）
```

---

## 四、文件放置规则

### 4.1 决策树

```
新增代码？
    │
    ├── 是纯计算逻辑？（无 HTTP/数据库）
    │   └── → core/
    │
    ├── 是配置或 gRPC 客户端？
    │   └── → shared/
    │
    ├── 是 HTTP API？
    │   └── → server/api/v1/ 或 v2/
    │
    ├── 是数据聚合/编排逻辑？
    │   └── → server/orchestrators/
    │
    ├── 是业务服务？
    │   └── → server/services/
    │
    ├── 是独立微服务？
    │   └── → services/{service_name}/
    │
    └── 是测试代码？
        └── → tests/
```

### 4.2 import 路径规范

```python
# 核心层
from core.calculators.bazi_calculator import WenZhenBazi
from core.analyzers.wangshuai_analyzer import WangShuaiAnalyzer
from core.data.constants import STEM_ELEMENTS
from core.config.ten_gods_config import TEN_GODS_RELATE

# 共享层
from shared.config.database import get_mysql_connection
from shared.config.redis import get_redis_client
from shared.clients.bazi_core_client_grpc import BaziCoreClient

# 服务层
from server.services.bazi_service import BaziService
from server.orchestrators.bazi_data_orchestrator import BaziDataOrchestrator
```

---

## 五、检查清单

创建新功能时，确认以下事项：

- [ ] 确定代码应放置的层级（core/shared/server/services）
- [ ] 检查依赖关系是否符合分层规则
- [ ] API 文件放在 `server/api/v1/` 或 `server/api/v2/`
- [ ] 纯计算逻辑放在 `core/`
- [ ] 配置和客户端放在 `shared/`
- [ ] 编排逻辑放在 `server/orchestrators/`
- [ ] gRPC 端点已注册到 `grpc_gateway.py`
- [ ] 测试文件放在 `tests/` 对应目录
- [ ] 文档已更新

---

## 六、迁移记录

### 2026-01-31 文档精简与规范统一

**变更内容：**
- 删除 `docs/` 目录，仅保留架构规范（`standards/`、`deploy/docs/`）中引用的文档
- 删除 `docs/knowledge_base/`、`docs/checklists/`、`docs/需求/`、`local_frontend/docs/`、`.cursorrules.d/`
- 删除前端相关文档（frontend-user、前端功能对应表等）
- `region_code_table.json` 移至项目根目录，脚本引用 `region_code_table.json`
- 未解析规则 JSON 输出路径：`scripts/migration/`（原 `docs/`）
- 规范文档统一入口：`standards/README.md`；部署文档：`deploy/docs/`

**引用更新：**
| 原引用 | 新引用 |
|--------|--------|
| `docs/knowledge_base/*` | `standards/` |
| `docs/checklists/` | `standards/05_功能开发检查清单.md` |
| `docs/需求/Coze_Bot_System_Prompt_*` | 在 Coze Bot 中直接配置 |
| `docs/region_code_table.json` | `region_code_table.json`（根目录） |
| `docs/未解析规则_*.json` | `scripts/migration/未解析规则_*.json` |

---

### 2026-01-18 架构重构

**变更内容：**
- 新建 `core/` 目录，迁移原 `src/` 下的核心计算逻辑
- 新建 `shared/` 目录，统一配置和客户端模块
- 新建 `server/orchestrators/` 目录，分离编排逻辑
- 删除 `src/` 目录
- 更新所有 import 路径

**迁移映射：**
| 原路径 | 新路径 |
|--------|--------|
| `src/bazi_calculator.py` | `core/calculators/bazi_calculator.py` |
| `src/analyzers/` | `core/analyzers/` |
| `src/data/` | `core/data/` |
| `src/bazi_config/` | `core/config/` |
| `src/clients/` | `shared/clients/` |
| `src/tool/` | `core/calculators/` |
| `server/config/mysql_config.py` | `shared/config/database.py` |
| `server/config/redis_config.py` | `shared/config/redis.py` |
| `server/services/bazi_data_orchestrator.py` | `server/orchestrators/bazi_data_orchestrator.py` |
