# HiFate-bazi 八字系统 - AI 开发规范（精简版）

> **完整规范**：`standards/README.md` | **部署指南**：`standards/deployment.md`、`deploy/docs/`

---

## 🔔 MCP 交互要求

**重要操作前必须征求用户意见**：
- 执行部署命令前（除非用户明确说「芝麻开门」即增量发布）
- 修改核心文件前（`grpc_gateway.py`、`bazi_calculator.py` 等）
- 删除文件前
- 执行不可逆操作前

**操作完成后必须告知用户**：
- 提供可选的后续操作建议
- 说明已完成的工作和影响范围

---

## 项目上下文

- **规范入口**：`standards/README.md`
- **部署**：`standards/deployment.md`、`deploy/docs/03-首次部署.md`
- **开发流程**：部署用 `@deploy.mdc`；提交前用 `@dev-checklist.mdc`

---

## 开发角色

高级资深全栈开发工程师。工作原则：性能优先、可观测性、防御性编程、架构思维。

---

## 核心原则（必须遵守）

### 0. 代码修改流程
> 本地开发 → Git提交 → **增量发布**（先 Node2 测试 23 接口 + 24 API，合适则 Node1 部署）。禁止直接在服务器上修改代码。
> 发布命令：`bash deploy/scripts/gated_deploy.sh`，详见 `@deploy.mdc`。
> Phase 2 初始化：`bash deploy/scripts/init_staging.sh`（一次性，零停机）。

### 0.1 零停机原则
> 使用热更新，禁止重启服务。

### 0.1.1 数据编排
> 数据只计算一次，所有接口从 `BaziDataOrchestrator.fetch_data()` 统一获取。禁止流式接口直接调底层服务、禁止绕过编排器。详见 `@orchestrator-rules.mdc` 或 `standards/08_数据编排架构规范.md`。

### 0.1.1.1 缓存
> 缓存命中率 ≥ 98%，key 禁止高精度时间戳（最多到小时）。详见 `@cache-rules.mdc`。

### 0.1.2 底层保护
> `core/`、`server/engines/`、`server/services/` 核心逻辑禁止修改。必须改时先告知用户。

### 0.2 热更新
> 所有更新必须热更新。禁止 `docker restart`。详见 `standards/hot-reload.md`。

---

## 开发原则（摘要）

1. 规范优先、安全优先、零停机优先
2. gRPC 优先（`standards/grpc-protocol.md`）
3. 规则数据库化（`standards/rule-development.md`）
4. 向后兼容、先测试后提交
5. **文件拆分用 shell 命令**（`cp`/`sed`/`head`/`tail` 搬代码，不逐字重写。减少 token 消耗和出错概率）
6. **生产代码隔离**：与生产无关的代码（测试脚本、基准测试、诊断工具、临时脚本）必须放在专门目录，不得与生产代码混在一起

---

## gRPC 与 API

- 前端走 gRPC-Web（`/api/v1/*`）
- 新增端点必须在 `grpc_gateway.py` 注册
- 修改 `grpc_gateway.py` 或 `proto/` 时用 `@grpc-rules.mdc`

---

## 项目架构与目录规范

### 生产代码目录（会部署到服务器）
- **server/**：Web 服务 (8001)，`grpc_gateway.py` 为核心
- **services/**：微服务 bazi_core(9001)、bazi_fortune(9002)、bazi_analyzer(9003) 等
- **core/**：核心计算逻辑、分析器
- **proto/**：gRPC 协议定义
- **deploy/**：部署脚本与配置

### 非生产代码目录（不会部署到服务器）
- **scripts/**：运维脚本、诊断工具、基准测试、临时脚本
  - 示例：`benchmark_*.py`、`diagnose_*.py`、`test_*.py`
- **tests/**：单元测试、集成测试、回归测试
  - 示例：`test_*.py`、`*_test.py`
- **tools/**：开发工具、数据生成器、辅助工具
  - 示例：`training_data/`、`data_migration/`
- **docs/**：文档、说明、分析报告
- **local_frontend/**：本地前端（生产前端由前端团队部署）

### 重要规则
> **任何与生产运行无关的代码，必须放在 `scripts/`、`tests/`、`tools/` 目录下，禁止放在生产代码目录（`server/`、`core/`、`services/`）中。**

---

## 核心文件（修改前必须咨询）

| 文件 | 风险 |
|------|------|
| `server/api/grpc_gateway.py` | 极高 |
| `server/engines/rule_condition.py` | 高 |
| `core/calculators/bazi_calculator.py` | 极高 |
| `proto/*.proto` | 高 |

---

## 下线代码管理

> 功能下线或替换时，旧代码必须及时标记并清理，禁止留"僵尸代码"被热更新扫描加载。

1. **标记**：在文件顶部和类/函数上方加 `# [DEPRECATED] 已被 xxx 替代，待删除` 注释
2. **去注册**：移除旧代码的装饰器注册（如 `@register_payment_client`、`@_register`），防止热更新 reload 时重新激活
3. **删除**：确认无引用后，下一个版本直接删除文件，不要长期保留废弃文件
4. **检查**：提交前搜索项目中是否存在同名注册（同一个 key 被多个文件注册），有冲突必须解决

---

## 禁止操作

- ❌ 服务器上直接改代码
- ❌ 跳过 Git 直接部署
- ❌ `docker restart` / `systemctl restart`
- ❌ 硬编码本地路径
- ❌ 从文件读取规则（必须从数据库）
- ❌ 下线功能只标 deprecated 不去注册（会被热更新重新加载）
- ❌ 将测试/诊断/临时代码放在生产目录（`server/`、`core/`、`services/` 等）
- ❌ 用户未明确要求时主动创建或修改 `.md` 文档（README、说明文档等）

---

## 用户意图触发器

> 用户说以下关键词时，**直接执行**对应命令，无需再确认（除非有未提交代码等阻碍）。
> **芝麻开门** = 增量发布流程（Node2 验证 23 接口 + 24 API → Node1 部署），详见 `docs/deploy/门控发布详细说明.md`。

| 用户说 | 执行 |
|--------|------|
| **芝麻开门** / **芝麻开发** | `bash deploy/scripts/gated_deploy.sh`（增量发布） |
| **芝麻开门 快速** | `bash deploy/scripts/gated_deploy.sh --skip-node2 --no-verify --non-interactive`（跳过 Node2+验证，直接 Node1 重启 web） |
| 芝麻开门 dry-run | `bash deploy/scripts/gated_deploy.sh --dry-run` |
| 芝麻开门 紧急 | `bash deploy/scripts/gated_deploy.sh --skip-node2` |
| 芝麻开门 回滚 | `bash deploy/scripts/gated_deploy.sh --rollback` |
| **测试** | `python3 scripts/evaluation/api_regression_test.py --env production --category basic --category stream --category payment --parallel` |
| 本地测试 | 同上，`--env local` |
| 热更新 | `python3 scripts/ai/auto_hot_reload.py --trigger` |
| 验证热更新 | `python3 scripts/ai/auto_hot_reload.py --verify` |
| 提交前检查 | 加载 `@dev-checklist.mdc` |

---

## 条件规则引用

| 场景 | 引用 |
|------|------|
| 部署/增量发布 | `@deploy.mdc` |
| 提交前检查 | `@dev-checklist.mdc` |
| 修改缓存 | `@cache-rules.mdc` |
| 修改 gRPC/API | `@grpc-rules.mdc` |
| 修改编排器 | `@orchestrator-rules.mdc` |
| API/测试 | `@frontend-test-and-backend-only.mdc` |

---

**完整规范**：`standards/`、`.cursorrules.backup`
