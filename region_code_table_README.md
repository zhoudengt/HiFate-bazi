# 地区码表使用说明

## 📊 数据统计

- **一级（国家/地区）**: 16 个
- **二级（省/州/区）**: 211 个  
- **三级（城市/街道）**: 546 个
- **四级（区域/社区）**: 1013 个
- **覆盖率**: 100% （所有城市都有四级数据）

## 📁 文件位置

- **码表数据**: `docs/region_code_table.json`
- **读取示例**: `docs/region_code_table_read_example.py`
- **数据统计**: `docs/region_code_table_stats.py`

## 🏗️ 数据结构

### 四级层级结构

```
一级：国家/地区 (regions)
  └─ 二级：省/州/区 (subdivisions)
      └─ 三级：城市/街道 (cities 或 major_cities)
          └─ 四级：区域/社区 (areas)
```

### 示例：香港中环

```json
{
  "regions": {
    "HK": {                          // 一级：中国香港
      "subdivisions": {
        "HK-HCW": {                  // 二级：中西区
          "cities": {
            "HK-HCW-CW": {           // 三级：中环
              "areas": {
                "HK-HCW-CW-IFC": {   // 四级：国际金融中心
                  "name_zh": "国际金融中心",
                  "name_en": "IFC",
                  "latitude": 22.2850,
                  "longitude": 114.1550
                }
              }
            }
          }
        }
      }
    }
  }
}
```

## 📖 如何读取数据

### 方法1：使用提供的函数

```python
from docs.region_code_table_read_example import *

data = load_region_table()

# 读取一级：国家/地区
hk = get_region_info(data, 'HK')

# 读取二级：省/州/区
hk_cw = get_subdivision_info(data, 'HK', 'HK-HCW')

# 读取三级：城市/街道
central = get_city_info(data, 'HK', 'HK-HCW', 'HK-HCW-CW')

# 读取四级：区域/社区
ifc = get_area_info(data, 'HK', 'HK-HCW', 'HK-HCW-CW', 'HK-HCW-CW-IFC')

# 列出所有四级区域
areas = list_all_areas(data, 'HK', 'HK-HCW', 'HK-HCW-CW')
```

### 方法2：直接读取JSON

```python
import json

with open('docs/region_code_table.json', 'r', encoding='utf-8') as f:
    data = json.load(f)

# 读取中环的所有四级区域
hk_cw_central = data['regions']['HK']['subdivisions']['HK-HCW']['cities']['HK-HCW-CW']
areas = hk_cw_central.get('areas', {})

for area_code, area_info in areas.items():
    print(f"{area_info['name_zh']}: ({area_info['latitude']}, {area_info['longitude']})")
```

## 🌍 覆盖地区

### 港澳台
- ✅ 香港：18个区，54个街道，69个四级区域
- ✅ 澳门：8个区，8个城市，10个四级区域
- ✅ 台湾：22个县市，109个乡镇区，223个四级区域

### 东南亚
- ✅ 新加坡：5个区，29个城市，32个四级区域
- ✅ 马来西亚：16个州，46个城市，85个四级区域
- ✅ 泰国：15个府，42个城市，87个四级区域
- ✅ 越南：11个省，18个城市，35个四级区域
- ✅ 印尼：11个省，20个城市，42个四级区域
- ✅ 菲律宾：7个区，17个城市，33个四级区域
- ✅ 缅甸：8个省，10个城市，12个四级区域
- ✅ 柬埔寨：5个省，5个城市，5个四级区域
- ✅ 老挝：4个省，4个城市，4个四级区域
- ✅ 文莱：4个区，6个城市，6个四级区域

### 北美
- ✅ 美国：54个州/领地，138个城市，286个四级区域
- ✅ 加拿大：13个省/地区，24个城市，52个四级区域
- ✅ 墨西哥：10个州，16个城市，32个四级区域

## 🔍 搜索功能

```python
# 搜索包含"中"的所有地区
results = search_by_name(data, name_zh='中')

for result in results:
    if result['level'] == 1:
        print(f"[一级] {result['name_zh']}")
    elif result['level'] == 2:
        print(f"[二级] {result['name_zh']}")
    elif result['level'] == 3:
        print(f"[三级] {result['name_zh']}")
    else:
        print(f"[四级] {result['name_zh']}")
```

## 📝 数据字段说明

每个条目包含以下字段：

- `code`: ISO 3166标准编码
- `name_zh`: 中文名称
- `name_en`: 英文名称
- `latitude`: 纬度
- `longitude`: 经度
- `timezone`: IANA时区（一级和二级）
- `utc_offset`: UTC偏移量（一级和二级）
- `type`: 类型（capital, municipality, county等）
- `parent`: 父级编码（如HK、MO的parent为CN）

## ✅ 验证数据完整性

运行统计脚本：
```bash
python3 docs/region_code_table_stats.py
```

运行读取示例：
```bash
python3 docs/region_code_table_read_example.py
```

## 📅 更新日期

- **版本**: 1.0.0
- **最后更新**: 2026-01-15
- **数据完整性**: 100% （所有城市都有四级数据）
